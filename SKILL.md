---
name: solo-create
description: "为本地代码项目生成、投递并迭代单轮或批量协作提示词。适用于用户希望读取单个项目代码，生成首轮提示词并进入验收；更常用于批量提示词已生成并投递后，在某个子项目目录直接进入验收；也适用于选择父目录批量生成提示词，写入 Excel，并按默认数量或指定编号范围自动打开 Trae、粘贴提示词并发送。"
---

# 项目出题循环

这套流程的目标只有一个：更快地产出贴项目、能直接复制、能持续迭代的提示词和验收结论。

本 skill 必须按“先定路由，再过门禁，再执行单一路径，再按唯一输出模板返回”的顺序运行。不要边做边换路，不要把说明性文字当成可选建议。

其中：

- `$solo-create` 仍然是用户唯一主入口。
- 命中 `批量建仓路` 时，必须调用平级 skill `$solo-batch-repo-seed`。
- 命中 `批量首轮路` 时，必须调用平级 skill `$solo-batch-first-prompts`。
- 验收或修复验收产出最终验收模板时，必须调用平级 skill `$solo-acceptance-results` 回填独立结果 Excel。
- 用户即使最终只使用 `$solo-create`，这些子 skill 也只作为内部流程下沉，不改变主入口。

## 0. 执行契约

### 0.1 强制状态机

每次运行都必须先选且只选一个 route。合法 route 只有这 6 个：

- `已投递验收路`
- `首轮半严路`
- `后续快路`
- `批量建仓路`
- `批量首轮路`
- `批量投递路`

没有选出 route 之前：

- 不要扫仓库。
- 不要打开 Trae。
- 不要读手册。
- 不要启动服务。
- 不要生成提示词。
- 不要给验收结论。

### 0.2 执行前自检

进入动作前，先在内部完成这 8 项自检：

- `route` 是什么。
- 当前是否是验收场景。
- 当前是单项目还是父目录批量场景。
- 当前 `browser_policy` 是默认降级到 `@chrome`/Safari，还是强制 `@浏览器`。
- 当前是否明确指定了无头浏览器模式（`acceptance_browser_mode=headless`），还是沿用默认可见浏览器模式。
- 当前代码侧证据是否已经足以直接判 `未完成`，从而必须命中 `2.10`。
- 当前是否已经收到本次对话里的合法 `Trae Session ID`，以及这次验收要走“带 Session 提交态”还是“无 Session 只验收态”。
- 当前最终允许输出哪些字段。

如果其中任一项无法确定，优先按本文件的 `Hard Stops` 执行；仍然无法确定时，只允许向用户提出一个最小澄清问题。

### 0.3 单项目起始动作

只要 route 是单项目流程，就必须先运行：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/ensure_trae_project_open.py --project-path .`

这一步优先级高于扫仓库、记基线、查历史、读手册、启动项目和浏览器验收。不要后移，不要懒触发，不要省略。

批量首轮路不执行这一步。批量投递路只在真正投递某个子项目时执行这一步。

### 0.4 核心数据格式

除了流程顺序，核心数据也必须先匹配 [data-contract.md](/Users/fengyin/.codex/skills/solo-creater/references/data-contract.md) 里的 schema，再进入 route 判断或最终输出。

必须使用并遵守这 7 组数据对象：

- `run_context`
- `project_lookup`
- `workbook_row`
- `acceptance_context`
- `dissatisfaction_block`
- `final_output`
- `acceptance_result_row`

执行要求：

- 进入动作前，先在内部把当前上下文收束成 `run_context`。
- 读取 `locate-project` 结果时，先映射成 `project_lookup`。
- 读取或更新 Excel 时，逐行按 `workbook_row` 理解，不要自造列。
- 进入验收时，先把上下文收束成 `acceptance_context`，再判断 `已完成 / 未完成 / 暂时无法判定完成`。
- 生成不满意原因和下一轮提示词时，必须使用 `$solo-dissatisfaction`，并先满足 `dissatisfaction_block` 的字段和角色分工。
- `$solo-dissatisfaction` 可以接收包含路径、文件名、函数名、命令名等技术细节的内部证据，但返回给最终验收模板的 `过程不满意`、`产物不满意` 和 `下一轮提示词` 必须是中文自然描述，不得出现文件路径、英文单词或原始代码标识。
- 生成最终验收模板后，必须使用 `$solo-acceptance-results`，并先满足 `acceptance_result_row` 的字段和结果 Excel 回填规则。
- 最终返回给用户时，只允许输出 `final_output` 允许的形态。
- 只要涉及 `Trae Session ID`，先按 [data-contract.md](/Users/fengyin/.codex/skills/solo-creater/references/data-contract.md) 里的 `0. Trae Session ID 格式` 校验；不合法就按“未收到合法 Session ID”处理。
- 只有本次验收明确走“带 Session 提交态”时，才检查 3 个环节完全一致：当前有效值、`git commit` message、最终质检结论输出值。

如果本文件与 `data-contract.md` 的字段定义冲突，一律以 `data-contract.md` 为准。

## 1. Route Selection

按下面顺序做 route 判定。命中即停，不再继续往下匹配。

### 1.1 已投递验收路

当满足下面全部条件时，必须进入 `已投递验收路`：

- 当前目录是单个工程。
- 运行：
  - `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py locate-project --project .`
- 返回 `found: true`。
- 记录里的 `提示词` 非空。

命中后：

- 不再生成首轮提示词。
- 不再重复历史验重。
- 不再重复读任务手册来出题。
- 把 Excel 里的 `提示词` 当成已发送给 Trae 的任务依据。

### 1.2 首轮半严路

当满足任一条件时，进入 `首轮半严路`：

- 用户明确要求“单个标准完整流程”。
- 用户明确要求“重新生成首轮提示词”。
- 用户明确要求“从头生成提示词”。
- 当前是单工程，且 `locate-project` 返回 `found: false`，且用户没有说明它属于批量 Excel 子项目。

### 1.3 批量建仓路

当用户要求在父目录下把唯一的源代码子目录批量铺成多个本地任务目录时，进入 `批量建仓路`。典型表达包括：

- 批量建仓。
- 批量建任务目录。
- 按源代码批量复制出多个项目目录。
- 初始化一批源码相同的工作目录。

命中后按 `6.1` 调用 `$solo-batch-repo-seed`，只在本地建目录和建工作簿，不创建 GitHub 仓库、不推送远端。

### 1.4 批量首轮路

当用户要求在父目录下遍历多个子文件夹、批量生成提示词或写入 Excel 时，进入 `批量首轮路`。

如果父目录下还只有一个源代码子目录，且还没有建出任务目录和工作簿，不要进入批量首轮路，必须改走 `批量建仓路`。

### 1.5 批量投递路

当父目录下已存在 `solo-create-prompts.xlsx`，且用户给出编号范围、前几个、打开对应 Trae 窗口或要求自动发送时，进入 `批量投递路`。

### 1.6 后续快路

当已经出过一次题，用户是在补问题、修问题、继续改结果时，进入 `后续快路`。

### 1.7 验收分流

当用户回来让继续、看结果、验收、生成完了时：

- 如果已经命中 `已投递验收路`，留在该 route 内执行验收。
- 如果当前处理的是首轮提示词后的结果检查，仍然留在对应单项目 route 内执行验收。
- 不要新造第 7 条 route。验收只是 route 内的阶段，不是独立 route。

## 2. Hard Stops

下面这些门禁优先级最高。命中后必须立刻停止主流程，只允许输出指定内容。

### 2.1 验收缺少合法 Trae Session ID

这不再是默认 STOP，而是验收模式分流。

只要进入验收阶段，就先判断本次是：

- `带 Session 提交态`：用户已提供格式合法的 `Trae Session ID`，允许按本文件的提交 / push 链路执行。
- `无 Session 只验收态`：用户没有提供合法 `Trae Session ID`，或明确表示这次不提供。

进入 `无 Session 只验收态` 时必须满足：

- 继续正常做代码验收、浏览器验收和最终结论输出。
- 一定不能执行 `git commit`、`git push`、`git amend`，也不要为了凑齐字段去生成或代填 `Trae Session ID`。
- 最终验收结果里不要展示 `Trae Session ID` 和 `Commit ID`。
- 如果当前项目属于 Excel 管理子项目，验收阶段也不要再往生成提示词 Excel 写入任何验收字段；只允许通过 `$solo-acceptance-results` 回填独立结果 Excel，缺少 `Trae Session ID` 和 `Commit ID` 时对应单元格写空。

只有当用户已经提供格式合法的 `Trae Session ID` 时，才允许进入 `带 Session 提交态`。

### 2.2 已投递验收路但 Excel 状态不允许继续

如果 `locate-project` 命中后，记录状态是：

- `已发送`：可以直接进入验收；有合法 `Trae Session ID` 就走 `带 Session 提交态`，没有就走 `无 Session 只验收态`。
- `已生成` 但不是 `已发送`：必须先提醒“这条提示词可能还没有投递”；但这不再阻塞验收，仍可按当前代码和结果进入验收。

这时不要直接给验收结论。

### 2.3 批量投递时 Trae 未聚焦到目标项目

如果当前窗口标题不包含目标子文件夹名称，或无法确认聊天输入框已经聚焦：

- 记录该条失败并跳过。
- 不要更新 Excel 状态。
- 不要把提示词发到不确定输入区域。

### 2.4 无法判定 source 项目编号

批量建仓路里，如果源目录名和父目录名都提取不出连续数字，且用户没有指定 `--source-number`：

- 立即停止。
- 只要求用户补 `--source-number`。
- 不要自己编编号。

批量建仓路里，如果父目录下的候选子目录不是恰好一个，或者唯一源目录没有绑定 Git：

- 立即停止，不要建任何目录、不要写工作簿。
- 未绑定 Git 时只提示用户：原始代码目录没有绑定 Git，请先初始化 Git 仓库再重新运行批量建仓。

### 2.5 路由无法确定

如果按 `Route Selection` 顺序执行后仍然无法确定 route：

- 只允许向用户提出一个最小澄清问题。
- 不要提前扫仓库或生成内容。

### 2.6 验收阶段 push / remote 阻塞

如果验收阶段出现下面任一情况：

- 远端不可达。
- 远端权限不足。
- 仓库未配置可用 upstream。
- `git push` 失败且当前无法恢复到正常推送。

本条只适用于 `带 Session 提交态`。如果当前是 `无 Session 只验收态`，根本不要进入 push / remote 流程，也不要因为 remote 状态阻塞验收输出。

必须立即停止验收结论输出。

这时只允许：

- 明确告知用户当前的 push / remote 问题是什么。
- 说明必须先恢复正常推送，之后才能继续给验收结果。

这时禁止：

- 输出 `任务完成情况`。
- 输出 `不满意原因`。
- 输出 `下一轮提示词`。
- 把 push / remote 阻塞写进 `过程不满意` 或 `产物不满意`。

### 2.7 需要浏览器但未使用 `@浏览器`、`@chrome` 或 Safari

这是浏览器验收场景的强制 STOP。

进入本条判断前必须先完成真实能力探测：

- 如果本次需要可见浏览器验收，先读取并执行 `browser:control-in-app-browser` 的 bootstrap，使用 Codex in-app Browser 的 `iab` 后端；不要因为聊天里没有独立的 `@浏览器` 工具名就直接判不可用。
- 只有 `iab` bootstrap、连接或关键操作失败，才算 `@浏览器` 当前不可用；必须记录具体失败点。
- 默认降级策略下，`@浏览器` 不可用后再读取并执行 `chrome:control-chrome` 的 bootstrap，使用 Chrome extension 后端；不要因为聊天里没有独立的 `@chrome` 工具名就直接判不可用。
- 只有 Chrome extension bootstrap、连接或关键操作也失败，才允许继续判断 Safari 兜底是否可用。

命中条件：

- 当前任务需要浏览器验收。
- 实际既没有使用 `@浏览器`，也没有在 `@浏览器` 不可用时改用 `@chrome`，也没有在 `@chrome` 不可用时改用 Safari。
- 或者打算改用 Google Chrome、Chrome 或其他系统浏览器。

命中后必须立即停止，不要继续验收，不要给完成结论。

命中后的唯一合法处理：

- 明确说明当前需要使用 `@浏览器` 验收；如果 `@浏览器` 当前不可用、连接失败或能力不足，则改用 `@chrome` 继续验收；如果 `@chrome` 也不可用，再改用 Safari。
- 如果 `@浏览器`、`@chrome` 和 Safari 都不可用，直接告知用户当前被什么问题卡住。
- 不要改用 Google Chrome、Chrome 或其他系统浏览器继续验收，除非用户在这次对话里明确指定。

### 2.7.1 无头浏览器验收

当且仅当用户在本次验收里明确指定 `acceptance_browser_mode=headless` 时：

- 允许使用无头浏览器完成验收。
- 不再因为未使用 `@浏览器`、`@chrome` 或 Safari 而触发 `2.7`。
- 无头模式下仍然必须完成页面进入、关键操作、结果落点、刷新后保持、返回或切换后的再次进入、异常输入或空数据下的表现这类验证。
- 如果无头浏览器启动失败、页面打不开、关键交互无法执行或无法读取结果，同样按验收失败处理。
- 如果用户没有明确指定该模式，就继续按默认可见浏览器模式执行。

### 2.8 当前线程未挂载 `@浏览器` 能力

这是浏览器验收前的能力门禁。

这里的“未挂载”只能来自真实探测结果：已经按 `browser:control-in-app-browser` 说明尝试通过 Node REPL 连接 `iab` 后端，但当前线程没有 Browser 插件后端、连接失败，或关键验收操作无法执行。不要只凭当前工具列表里没有名为 `@浏览器` 的工具就命中本条。

命中条件：

- 当前任务需要浏览器验收。
- 当前这次对话实际没有 `@浏览器` 可调用能力，或已经明确出现“当前对话没有使用 `@浏览器` 的能力”这类提示。
- 当前不是无头浏览器验收模式。

命中后处理：

- 先说明当前线程未挂载 `@浏览器` 能力。
- 如果当前 `browser_policy=prefer_browser_allow_chrome_fallback`，先按 `chrome:control-chrome` 说明真实尝试连接 Chrome extension 后端并继续浏览器验收；如果 Chrome extension 也不可用，再用 Computer Use 操作 Safari 兜底。
- 如果当前 `browser_policy=browser_only`，立即停止，并明确要求切换到带 `@浏览器` 能力的新线程后再继续。
- 如果 `@chrome` 和 Safari 都不可用，再说明当前浏览器验收被什么问题卡住。
- 不要自行改用 Google Chrome、Chrome 或其他系统浏览器，除非用户在这次对话里明确指定。
- 如果是无头浏览器验收模式，按无头模式继续执行，不要把缺少 `@浏览器` 当成阻塞。

### 2.9 强制 `@浏览器` 模式下 Browser 不可用

这是浏览器验收场景的强制 STOP。

强制模式下也必须先真实尝试 `browser:control-in-app-browser` 的 `iab` bootstrap。只有 bootstrap、连接或关键操作失败后，才允许按本条停止；不要因为当前消息没有显式唤起 `@浏览器` 或工具列表里没有这个别名就提前停止。

命中条件：

- 当前任务需要浏览器验收。
- 当前 `browser_policy=browser_only`。
- 当前线程未挂载 `@浏览器` 能力，或 `@浏览器` 连接失败、能力不可用、无法完成本次验收。

命中后唯一合法处理：

- 明确说明这次验收被强制限定为只允许 `@浏览器`。
- 明确说明当前线程没有可用的 `@浏览器` 能力，或当前 `@浏览器` 被什么问题卡住。
- 明确要求用户新开一个带 Browser 插件的独立线程，并把首条消息写成浏览器验收任务。
- 不要降级到 `@chrome` 或 Safari。
- 不要改用 Google Chrome、Chrome 或其他系统浏览器。
- 不要继续给验收结论。

### 2.9.1 无头浏览器模式不可用

如果用户明确指定了 `acceptance_browser_mode=headless`，但无头浏览器无法启动、无法连接、页面无法打开，或关键交互无法执行：

- 立即停止无头验收。
- 明确说明当前卡在无头浏览器能力或启动问题上。
- 不要自动切回 `@浏览器`、`@chrome` 或 Safari，除非用户重新授权切换模式。
- 不要继续给验收结论。

### 2.10 代码已足以判 `未完成` 却仍试图启动服务或进入浏览器

这是验收分级前的强制 STOP。

命中条件：

- 当前已经进入验收阶段。
- 通过代码变更、测试结果、类型检查、命令行验证或直接相关文件内容，已经足以支持 `未完成` 结论。
- 后续仍打算启动本地服务、进入 `标准验收`、进入 `重验收`，或使用任何浏览器补页面证据。

命中后唯一合法处理：

- 立即停止后续服务启动和浏览器验收动作。
- 明确按代码侧证据直接输出 `未完成` 或继续组织 `未完成` 所需的验收结论字段。
- 不要再进入 `标准验收`。
- 不要再进入 `重验收`。
- 不要再使用 `@浏览器`、`@chrome` 或 Safari 补验证。

## 3. 输出级硬禁令

最终要发给用户、并且用户可能直接复制使用的输出级内容，必须先过这一层。输出级内容包括：

- 生成的提示词。
- `过程不满意`。
- `产物不满意`。
- 跟这三类内容紧挨着的简短结论补充。

这些内容里严禁出现：

- `稳定`
- `不稳定`
- `可靠`
- `不可靠`
- `收口`
- `收住`
- `落下来`
- `这一轮`
- `上一轮`
- `当前轮次`
- `同上`
- `（继续）`

如果一句话必须表达“基于这次结果继续修”，也要直接改写成当前看到的问题和要补的行为，不要借助跨次承接词。

输出级内容还有 3 条格式硬规则：

- `过程不满意` 不能以页面、按钮、文案、提示或内容显示英文作为理由；英文显示属于产物现象，只能放进 `产物不满意`。
- `过程不满意`、`产物不满意` 和 `提示词` 每一行在换行前都必须以标点结尾，优先用 `。`、`！`、`？`。
- 如果输出了 `下一轮提示词`，它必须以 `修复` 两个字开头。
- `过程不满意`、`产物不满意` 里禁止使用 `稳定`、`不稳定`、`可靠`、`不可靠` 这 4 个词；需要表达结果反复、行为飘忽、缺少确定性时，必须改写成具体可观察的现象、触发条件和业务影响。
- 只要 `过程不满意` 或 `产物不满意` 命中 `稳定`、`不稳定`、`可靠`、`不可靠` 任一词，当前输出立即视为无效，必须当场整段重写；不要带着命中的版本继续往下输出。

## 4. 总原则

- 只要进入验收，默认先做结果验收。
- 验收完成后，必须通过 `$solo-acceptance-results` 把结果回填到当前子项目目录的直接父目录里的独立结果 Excel；例如子项目是 `.../solo-6600021/66000212-codegen-2`，结果 Excel 就写到 `.../solo-6600021/solo-create-acceptance-results2.xlsx`；禁止把验收结果、Repo URL、Commit ID、Trae Session ID 或不满意原因写入 `solo-create-prompts.xlsx`。
- 结果 Excel 与生成提示词 Excel 必须分离；默认结果文件名是 `solo-create-acceptance-results2.xlsx`，第一次不存在时由 `$solo-acceptance-results` 新建。
- 验收默认目标是优先找出“未完成”的直接证据，不以替实现补理由，也不以凑出“已完成”结论为目标。
- 只要是前端页面、前端交互、可视化结果、浏览器流程或用户能点到的链路，默认按“需要浏览器严格实操”处理；除非代码侧已经足以直接判 `未完成`。
- 只要通过代码变更已经足以判定当前验收结论是 `未完成`，就必须当场停止在代码验收层，不要启动本地服务，不要进入任何浏览器验证，也不要再补做“确认一下页面”的动作。
- `@浏览器` 是默认优先使用的浏览器验收工具；执行时必须先按 `browser:control-in-app-browser` 连接 Codex in-app Browser 的 `iab` 后端。只有真实连接或操作失败后，才按 `@chrome` 降级；`@chrome` 必须按 `chrome:control-chrome` 连接 Chrome extension 后端。只有 Chrome extension 也真实不可用时，才允许再用 Computer Use 操作 Safari 兜底。
- 如果用户明确指定无头浏览器模式，则优先使用无头浏览器执行验收；无头模式不影响默认的结果验收顺序和文件回填要求。
- 如果用户在本次消息里明确要求“100% 使用 `@浏览器`”“只允许 `@浏览器`”“不要 `@chrome` 或 Safari 降级”，就把 `browser_policy` 设为 `browser_only`，并按 `2.9` 执行硬停。
- 不要把 Google Chrome、Chrome 或别的系统浏览器当成默认等价替代。
- 只要验收需要本地启动前端服务，就必须把“端口占用时顺延找可用端口启动”和“输出结论后停掉这次验收启动的服务”当成强约束执行，不能省略。
- 浏览器验收时必须按真实用户路径逐步点击、输入、切换、刷新、回退、重进，不要只看首屏，不要只凭截图，不要只做一次成功路径就结束。
- 只要出现任何关键行为缺失、状态不同步、交互歧义、文案与行为矛盾、刷新后失效、回跳异常、列表详情联动不一致、边界输入异常、空态异常、报错未兜底，默认判 `未完成`。
- 只有用户这次消息里真的贴了 Trae 过程信息，才叠加做过程验收。
- 如果结果没问题，但过程里明显有冗余思考、待办拆法很怪、语言跑偏、一直重复解释、执行节奏很乱，也不能直接算高质量完成。
- 如果用户没贴 Trae 过程信息，就不要凭空编造过程记录；但只要结果不满意，`过程不满意` 仍然必须按本文件的质量规则完整成立，不再要求“简短反推”。

### 4.1 提高 `@浏览器` 命中率

当后续明显会进入浏览器验收时，优先这样组织线程和首条消息：

- 尽量新开一个独立线程，不要把读代码、改代码、批量处理和浏览器验收全塞进同一条长上下文。
- 首条消息直接点名 `@浏览器`，不要只写“打开页面”或“帮我验收”。
- 如果目标是 100% 只用 `@浏览器`，首条消息里必须明确写“只允许 `@浏览器`，不要 `@chrome` 或 Safari 降级”。
- 首条消息里尽量同时给出可访问目标，例如 `http://localhost:3000`、`http://127.0.0.1:5173` 或明确的页面入口。
- 优先把任务表述成浏览器操作任务，例如“用 `@浏览器` 打开某地址并按真实用户路径验收某流程”。
- 默认策略下，如果当前线程已经提示缺少 `@浏览器` 能力，先确认是否已经真实尝试 `iab` bootstrap；如果没有，先尝试 `iab`。确认失败后再尝试 Chrome extension；只有 Chrome extension 和 Safari 都不可用时，再建议用户新开线程重试。
- 强制策略下，如果当前线程已经提示缺少 `@浏览器` 能力，也必须先确认是否已经真实尝试 `iab` bootstrap；确认失败后不要继续验收，直接要求用户新开一个带 Browser 插件的线程。
- 无头模式下，首条消息或验收上下文里要明确写出 `acceptance_browser_mode=headless`，避免和默认可见浏览器模式混淆。

## 5. 单项目路径

### 5.1 标准完整流程

仅当用户明确要求从头生成首轮提示词时，按下面固定顺序执行：

1. 先执行 `ensure_trae_project_open.py`。
2. 进入 `首轮半严路` 生成首轮提示词。
3. 最终只输出提示词正文，不要夹带别的说明。
4. 同一次执行内直接进入验收阶段。
5. 如果验收未通过，切到 `后续快路` 生成修复提示词。

### 5.2 已投递验收路

进入单工程目录后，先运行：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py locate-project --project .`

如果返回 `found: true`，且记录里 `提示词` 非空：

- 不再生成首轮提示词。
- 不再重复历史验重。
- 不再重复读任务手册来出题。
- 把 Excel 里的 `提示词` 当成该项目已发送给 Trae 的任务依据。
- `状态` 为 `已发送` 时，有合法 `Trae Session ID` 就走 `带 Session 提交态`，没有就走 `无 Session 只验收态`。
- `状态` 为 `已生成` 但不是 `已发送` 时，先提醒可能还没有投递；但不再要求必须等待用户发送合法 `Trae Session ID` 后才能进入验收。
- 验收没过时，按 `后续快路` 生成修复提示词。

如果返回 `found: false`：

- 说明当前工程不是批量 Excel 管理的子项目。
- 除非用户另有说明，否则退回 `标准完整流程`。

### 5.3 首轮半严路

首轮不走最重流程，但也不能只凭经验出题。必须按下面顺序执行：

1. 先执行 `ensure_trae_project_open.py`。
2. 轻扫仓库，优先用：
   - `rg --files`
   - `git status --short`
   - `git diff --stat`
3. 只读和当前切入点直接相关的文件。
4. 判断任务类型；明确就直走，不明确才让用户选。
5. 运行历史验重：
   - `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/query_prompt_history.py --project-path . --task-type "<任务类型>"`
6. 按任务类型只读命中的一本手册，不要把全部手册都读一遍。
7. 尝试记基线：
   - 优先运行仓库里的 `scripts/record_round_baseline.py`
   - 没有再回退到 skill 自带脚本
   - 如果缺失或失败，要明确告知当前是降级处理，后续验收只能基于累计改动判断
8. 生成符合“首轮提示词要求”的中文提示词。
9. 执行输出级禁词检查。
10. 做难度下限实测，命中两项及以上「过于简单」特征时换角度重出：
    - `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_difficulty.py --prompts-file "<把本次最终提示词写进临时文件>"`
11. 记录提示词历史，并带上难度档位和这次用到的隐藏约束：
    - `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/record_prompt_history.py --project-path . --task-type "<任务类型>" --angle "<这次的切入点>" --difficulty "<地狱|困难>" --hidden-constraints "<隐藏约束中文描述；没有就传空>" --prompt "<最终提示词>"`

首轮去重规则必须执行：

- 重点看同前缀、同类型、同 angle 的历史。
- 如果 `matched_count` 大于 `0`，优先换 angle，不要只改几个词。
- **必须先跑同仓库主题去重（平台查重规则 C 的本地预检）**，而且要带上仓库：
  `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --parent "<父目录>" --repo "<父目录>/<任意任务目录>/origin" --require-ledger --write-ledger`
  （单项目没有父目录时用 `--prompts-file`）。
- **规则 C 的真实口径（2026-09-16 拉全平台判词复盘，97 条提交 / 41 条废弃，其中 38 条是规则 C）**：
  平台判的是**主体 + 需求模式**，不看措辞，也不看这个模块在仓库里存不存在。
  - 字面相似度不作数：判废对最低 9.5%，判词原话「只改写措辞无效」；
  - 同主体 + 同模式就算废，功能点完全不同也照判：「同为围栏模块的属性扩展，功能点不同」
    「共同主体是生成受控只读访问入口，但一为现场扫码巡检登记、一为监控视图只读分享」；
  - **跨主体同模式也判废**：判废对里 29%（cc-6600009）到 59%（cc-6600011）是跨模块的同模式对；
  - **0-1 代码生成不豁免**：cc-6600009 那批 17 条代码生成废了 10 条（59%），比功能迭代还高，
    21 对判废对里 10 对两边都是 0-1 代码生成。凭空造的功能只要落在同一主体或同一需求模式上照样废；
  - **类型配比是最可控的杠杆**：新增能力类（0-1 代码生成 + 功能迭代）废弃 43%–57%，
    缺陷修复 14%–29%，重构/理解/工程化 0%。
- **主体词典必须从仓库自身派生**（`--repo` 指向 `origin`）：脚本按组件与服务文件里的中文串
  建词典，一个组件就是一个主体（RecordingPanel、BrainStateDashboard、BandPowerChart…），
  题面必须能落回某个主体；`--max-unknown` 默认 0，落不回去的题面要人工确认改的是哪一块
  （工程化、代码理解、代码重构三类自动豁免）。**主体识别为空仍然报 ok 是最危险的失败模式**，
  脚本现在会直接判不通过。
- **题位表是硬前置**：`--write-ledger --write-feature-points` 会把每条的「主体 + 需求模式」
  写成题位台账（父目录 `repo-theme-ledger.json` 与 `repo-feature-points.json`）。
  出题前**先按题位表分配座次**，一个格子只能坐一条题，写不出差异就换主体或换模式，
  不要先写完几十条再去查重。
- **硬约束（跨批次累计）**，2026-09-16 复标定后的口径（方案 B）：
  **同一主体 ≤ 2 条且两条必须跨任务家族**（`--max-per-subject` 默认 2）、
  **同一需求模式 ≤ 2 条**（`--max-per-mode` 默认 2）、单仓库 ≤ `--max-per-repo`（默认 35）、
  单个 6 字句式片段占比 ≤ `--max-ngram-ratio`（默认 20%）、
  新增能力类占比 ≤ `--max-new-capability-ratio`（默认 50%）。
  任务家族只有三类：**新增能力**（代码生成 / 功能迭代）、**缺陷修复**（缺陷修复 / Bug 修复）、
  **结构改造**（代码重构 / 代码理解 / 工程化）。同一个主体的第二条题必须换家族。
- **为什么是「2 条 + 跨家族」而不是「1 条」**（38 对真实判废对的实测）：
  - 每对判废率：同主体对 **4.7%–7.7%**、跨主体对 **0.85%–1.02%**；
  - 按家族拆开：同家族同主体被判废的有 代码生成↔代码生成 14 对、代码生成↔功能迭代 11 对、
    功能迭代↔功能迭代 10 对、缺陷修复↔缺陷修复 3 对；**「新增能力类 ↔ 缺陷修复」跨家族组合 0 对**；
  - 所以放开第二条、但强制跨家族，是最划算的扩容：cc-9900003 那种 11 个主体的仓库
    产能从 11 条提到 **22 条**，预期废弃仍只有 0–1 条。
  - 产能 = `min(单仓库上限, 主体数 × 每主体上限, 模式数 × 每模式上限)`；
    要零风险就显式收回到 `--max-per-subject 1 --max-per-mode 1`（那批就是 11 条）。
- **护栏有三道，对象轴只当复核信号**：
  - 硬拦：**同主体同家族**（同主体两条必须跨家族）、**同模式 >2**、**6 字句式片段 >20%**、新增能力类 >50%；
  - 复核信号：`review_list` 里的 `object_overlap`（同主体两条的对象词重合数）——
    这一项默认不拦，因为对象词是 2/3 字片段，噪声太大（「一条」「以后」「几个」也算），
    放开第二条后它会把 11 对同主体对全部拦下、等于回到 1 条/主体；要更严可以加
    `--min-shared-object-words 2` 把它升级成硬拦。
  - 只靠「同主体同家族 + 同模式」这两条，对 38 对真实判废对的召回是 **37/38 = 97.4%**，
    与放开前持平。
- **跨家族这条是「0 对反例」支撑的经验规则，不是定理**：38 对样本量下，真实风险上限约 8%，
  所以第一次按 2 条/主体出题时**先探路**：挑 5–8 条含「同模块跨家族对」的题先提交一批，
  判词回来确认放行再放量；判词回灌到 `coding-agent-satisfaction-annotation` 的语料库。
- **句式指纹是新增的一道闸**：整批复用同一个从句就是判词里的「约束句式一致」。
  实测 cc-9900003 那批 46 条里有 74% 含同一个「刷新后与返回后」从句，而两个被判废的批次
  最高只有 10%——所以「把边界与一致性写全」这件事很容易把整批写成同一个模板。
  同一个从句在整批里最多占 20%，要拆成多种说法轮换。
- **硬拦现在能自证**：拿 38 对真实判废对回测，「同主体或同模式即拦」的硬拦召回
  **97.4%**（cc-6600009 20/21，cc-6600011 17/17）。剩下的 1 对是跨主体又不同模式的，
  交给复核清单。`review_required_labels` 里每条题仍要写出**与最近邻的差异**，
  但正常情况下这个清单会小得多——如果它又长出几十条，说明主体或模式在同批里重复了，
  先回去按硬拦换题位。
- **判词回归门**：闸门默认加载 `tests/fixtures/rule-c-corpus.json`（97 条实际提交过的题面 +
  38 对平台点名的雷同对），判据召回低于 **95%** 直接判不通过。改判据必须过这道门，
  不要靠「我觉得更严了」。
- **这条规则现在有机械闸门（2026-09-16 起）**：提示词写进工作簿走的是
  `batch_prompt_workbook.py update`，它会在落表前把整批内容送进三道闸——同仓库主题去重、
  难度下限、提示词查重；任何一道不过就**直接拒写**，只打印违规清单，只有显式加
  `--allow-gate-failure` 才能强写。2026-09-16 的 cc-6600025 那批就是在「规则已推、闸门没人跑」
  的缝里生成的（同模块塞了 7 条、4 个模块超配额），所以别再依赖「记得跑」。
  - 写入成功后 `备注` 会带上 `生成规则: <闸门版本>；闸门: ok`，父目录同时落一份
    `prompt-generation-manifest.json`（闸门版本、主体/模式分布、类型配比、判词回归结果、
    需复核清单、违规清单、skill 版本），跨机器可追溯。
  - `update` 还会校验 skill 是否落后于远端：落后就拒写并要求先 `git pull --ff-only`
    （确实要用当前版本加 `--skip-version-check`）。
- **台账按仓库累计，不跟着父目录走**：父目录台账之外还有一份同仓库台账
  （默认 `~/.codex/repo-theme-ledgers/<owner-repo>.json`，可用
  `SOLO_CREATE_REPO_LEDGER_ROOT` 改位置），换一个父目录继续出题时配额与题位照样累计。
  出题前先生成题位表：`check_repo_theme.py --parent "<父目录>" --repo "<origin>" --write-ledger --write-feature-points`
  会写出 `repo-feature-points.json`（主体词典 + 需求模式 + 已占用的「主体 × 模式」格子），
  按它分配题位再写题面。
- 同一主体最多 2 条且两条必须跨任务家族、同一需求模式最多 2 条（硬约束，跨批次累计），单仓库不超过 35 条；
  超了必须换主体、换模式或换仓库出。
  平台是拿先提交的那条当基准、后提交的判废弃，所以同仓库要一次性全局去重。
  提交侧配合分批（每批 ≤10 条）兜底：即使有漏网，损失也锁在一批里，不会一次废十几条。
- 如果当前类型是 `缺陷修复`，首轮必须凑齐两个彼此咬合的缺陷点，或一个共享根因在两个以上入口都能看到，不要交单点小修。
- 不要沿用“新增 xxx 功能，支持……并……”这类高频骨架反复换皮。
- 不要把历史提示词的动作链顺序原样保留后只替换对象词；发现主句结构接近时，直接换整句组织方式。

首轮提示词必须满足：

- 必须贴当前仓库。
- 必须是用户可感知的问题或需求。
- 默认用中文。
- 不要出现文件路径、函数名、类名、实现指挥。
- 不要写成任何项目都能套用的通用模板。
- 如果任务类型是 `代码生成`，默认从新增能力本身起笔，优先写成 `新增 xxx 功能，……`。
- 不要用“现在只有……，希望新增……”这种把现状铺在前面的句式。
- 只有不补一句背景就无法理解新增能力时，才允许补极短业务上下文。
- 除 `功能迭代` 外，如果一句提示词删掉“目前 / 现在 / 已有 / 现有 / 原来”后仍然成立，默认删掉再输出；`功能迭代` 最多保留一句现状锚点，只用于说明扩展对象。
- 全文不超过 250 字（空白字符不计入），约束要求融入正文，不得单独分行或加标签前缀。

首轮提示词的复杂度必须满足：

- 一条提示词至少包含 2 个相关能力点，`地狱` 档位要压到 2 到 3 个互相咬合的能力点；只描述一个孤立能力的题不要交。
- 至少覆盖 1 类非开心路径（空数据、失败重试、边界输入、重复提交、并发时序、权限差异等）；`地狱` 档位至少覆盖 2 类。
- 至少包含 1 项与既有功能或既有状态的联动要求（刷新后保持、返回后恢复、列表与详情同步、历史记录同步等）；`地狱` 档位必须包含跨页面或跨状态的一致性要求。
- 至少包含 1 项不破坏既有行为或兼容既有数据的要求。
- 单靠一个文件就能完成的题不要交，必须至少牵动两个以上彼此相关的文件层级。

难度下限硬门禁（平台「过于简单」拒收口径）：

- 平台规则是「同一道题同时命中两项及以上过于简单特征时一律拒收」，必须按下面这张表逐项自查，命中项压到 1 项以内，其中 `修改范围` 不允许命中：
  - `修改范围`：改动收在一处判断、一个文件或一段文案里，题面没有任何跨入口、跨数据、后续操作或既有状态联动要求。
  - `交互轮次`：一次点击或一次提交就能验完，没有后续步骤、返回、刷新、切换、重新进入这类多轮动作。
  - `技术广度`：只落在一个技术面（常见是只有输入校验），没有状态与持久化、数据与接口、并发与时序、权限与角色这些第二个面。
  - `逻辑复杂度`：只有单一条件判断（顺序、空值、长度、格式、大小），没有复合条件或与其它业务规则的咬合。
  - `验证深度`：只有开心路径，既没有非开心路径，也没有跨状态一致性要求。
- `缺陷修复` 首轮必须先过 `references/01_Bug修复任务执行手册.md` 的「难度下限」一节，再合成题面；只写一处校验加一句提示的题一律不要交。
- 必须实测：`python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_difficulty.py --prompts-file "<把本次最终提示词写进临时文件>"`；返回 `ok: false` 时必须换切入点和主诉求重出，只加限定词不算重出。
- **难度检查默认覆盖全部任务类型（2026-09-16 改）**：以前只查 `缺陷修复`，cc-6600011 的 33 号是
  `代码生成`，题面只有「按位置把通道摆出来 + 点一下切换 + 悬停显示名字」，平台按 P3
  「交互轮次 + 技术广度」两项拒收，本地那道闸门根本没轮到它。现在 `--task-types` 默认 `all`。
- **两类任务的结果不一样**：`缺陷修复` 仍然硬拦（命中 ≥2 项或结构下限不满足直接不通过）；
  `代码生成` / `功能迭代` 这套词面口径命中率偏高（同一批 49 条里 35 条命中，其中大部分被平台放行），
  所以改成进 `needs_review` 列表：出题人要逐条复核，把「这道题为什么不算简单」写进工作簿 `备注`，
  以 `难度复核: ` 开头留档。**看到 needs_review 就当没看见是最典型的漏检**——33 号那种题
  复核时一眼就能看出「一次点选 + 单一技术面」，该换切入点就换。
- **「修改范围」的硬信号命中即拒**（2026-09-16 起）：题面出现「只改一处」「单个文件/组件」「小改」「补一个」这类写法，脚本直接拒收，不再允许用「只命中一项」蒙混。平台 P3 判词（cc-6600009 的 41 号）就是拿这一条当依据的。
- **缺陷修复题有结构下限**：脚本会数缺陷点与入口，要求「至少两个咬合缺陷点，或一个共享根因跨两个以上入口」并且「题面落到两个以上入口」，缺一项就拒收；确实要跳过加 `--no-defect-structure`，但批量和首轮一律不许跳。
- 脚本是词面兜底，返回 `ok: true` 也必须按上面的下限动作再核对一遍语义，不要因为脚本通过就交单点题。

隐藏约束规则：

- 允许把一部分验收条款留在题面之外，但隐藏约束必须同时满足：验收阶段能客观判定；是合格工程师面对该需求时的合理默认预期；不与题面明示内容冲突。
- 每条提示词最多 2 条隐藏约束，禁止把核心需求本身藏起来。
- 题面里不得出现“隐藏”“验收会检查”这类元话术。
- 隐藏约束必须通过 `record_prompt_history.py --hidden-constraints` 记录，供验收阶段按同一份清单核对。

提示词重复硬阈值：

- 与同项目历史提示词之间，文字重复率必须 `<= 20%`（字符 3-gram 重合率，口径是两条提示词里较短一方被另一方覆盖的比例）。
- 语义近似度必须 `<= 20%`：脚本按字符 2-gram 的 TF-IDF 余弦给出客观值，自己还要再做一次语义复核，两边取更严的。
- 复合需求交叉重叠必须为 0 对：两条提示词共享 2 个以上业务能力点（筛选、排序、搜索、导出、统计汇总、权限角色、通知提醒、批量操作等）即判交叉重叠；出现率超过 30% 的高频能力标签只记录、不判违规。
- 必须实测，命令是 `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_dedup.py --include-history --prompts-file "<把本次最终提示词写进临时文件>"`，或者按批量场景直接用 `--parent "<父目录>"`。
- 返回 `ok: false` 时必须换角度重出，只改同义词不算通过；重出后重新跑检查，直到 `ok: true` 才能输出。

类型判断默认规则：

- 新增一块用户能独立感知的新能力、新入口或新流程，默认按 `代码生成`。
- 在已有能力上补规则、补交互、补展示，或在原流程上往前推一点，默认按 `功能迭代`。
- 不要因为题面里提到了某个现有页面、现有模块或现有入口，就把本该是 `代码生成` 的题写成 `功能迭代`。
- **反过来也不能把迭代题写成代码生成（2026-09-16 按 cc-6600011 的 21、25、31 判词补）**：
  这三条题面都是「在既有界面上新增一种能力」（访客观察模式、键盘操作、中英双语显示），
  工作簿里却按目录名填了 `代码生成`，平台按跨域错配判待返修，返修时改成 `功能迭代` 才过。
  批量生成时不要只看 pending 队列的分块，**写完提示词之后要按题面语义逐条复核任务类型**，
  和出题侧目录名、验收侧 `TASK_TYPE_MAP` 三处对齐；三类任务（代码生成 / 功能迭代 / 缺陷修复）
  的分块只决定排期，不决定最终类型。

### 5.4 后续快路

后续修复提示词默认走更短链路，不再重复首轮那套重动作。必须执行：

1. 先执行 `ensure_trae_project_open.py`。
2. 先看相对基线改动。
3. 先看上次验收确认过的问题点。
4. 只围绕已确认问题生成修复提示词。
5. 做难度下限实测，命中两项及以上「过于简单」特征时换角度重出。
6. 发给用户前，再做一次输出级禁词检查。

默认跳过：

- 不再重复做历史验重。
- 不再重复做任务类型选择，除非任务性质明显变了。
- 不再重读整本手册，除非输出风格明显跑偏。
- 不再重扫整仓库，只补读直接相关文件。

命中任一情况时，回到更严路径：

- 修复范围明显扩散，不再只是补问题。
- 用户中途换了需求方向。
- 基线缺失，累计改动已经看不清边界。
- 连续两次验收都发现问题描述和实际改动对不上。

修复提示词必须满足：

- 只写已经验出来的问题。
- 不要把最初大需求整段重写一遍。
- **不要把首轮那套需求换个说法再写一遍（2026-09-16 按 cc-6600011 的 45 号判词补）。**
  平台对修复轮记录按子规则 A-4「同义改写（等长换皮）」判废弃，不可返修：45 号第二轮的提示词
  就是把首轮需求复述了一遍，平台判词里的相似度 48.7%。本地口径与首轮提示词比对，
  文字重复率（字符三元组覆盖）与语义近似度（二元组余弦）都要 `<= 20%`；
  `check_prompt_dedup.py` 把两轮提示词放一起跑就能看出来，验收侧的
  `validate_repair_prompt.py --first-round-file` 与 `repair_round.py` 也会在续跑前拦一次。
- 必须以 `修复` 两个字开头。
- `修复` 后面必须立刻接当前要修的问题对象或用户可感知问题。
- 直接描述要补的行为结果，不要写过程指挥。
- 不要和这次 `过程不满意` 形成近似复述。
- 不要和这次 `产物不满意` 形成近似复述。
- 不要和本项目历史修复提示词只做同义词替换。
- 修复提示词同样受难度下限约束：即使已确认的问题本身只是一处判断，也要把同一根因影响的其它入口、返回或刷新后的表现、空数据或失败场景一起写进修复目标，不允许把修复写成“加一条校验再给一句提示”。
- 必须实测难度下限：把这条修复提示词写进临时文件后运行 `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_difficulty.py --prompts-file "<临时文件>"`；命中 2 项及以上「过于简单」特征时必须换角度重出。
- 修复提示词同样要过「修改范围」硬信号与缺陷修复结构下限；只写一处校验加一句提示的修复题一律不要交。
- 必须通过与首轮同一套重复硬阈值：把这条修复提示词写进临时文件后运行 `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_dedup.py --include-history --prompts-file "<临时文件>"`，文字重复率、语义近似度都要 `<= 20%`，复合需求交叉重叠为 0 对，`ok: false` 时换角度重出。
- 修复题不要再往已经占满的题位上出：先跑
  `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --parent "<父目录>" --repo "<父目录>/<任意任务目录>/origin" --require-ledger`，
  确认目标主体没超 2 条（第 3 条不行）、同主体两条跨了任务家族、目标需求模式没超 2 条、
  也没有落在同一主体同一业务对象上；
  撞格时同仓库的修复题同样会被规则 C 判废弃，不可返修。

## 6. 批量路径

### 6.1 批量建仓路

当父目录初始化时只有一个源代码子目录，而用户要求批量铺成多个本地任务目录时，走这条路。

这条路只做本地动作：把唯一子目录当作原始代码，校验它绑定了 Git，**先算这个仓库能出多少条题**，再按这个数字建出对应数量的任务目录（每个任务目录固定含 `origin` 与 `workspace`），并在父目录新建 `solo-create-prompts.xlsx`，行数与目录数一致。不创建 GitHub 仓库，不推送远端，不改动源目录。

**建仓数量从 2026-09-16 起由容量决定，不再固定 46 条**：

- 容量 = `min(单仓库上限 35, 主体数 × 每主体上限 2, 需求模式数 × 每模式上限 2)`。
  平台是在同一个仓库里两两比对的：判废对里 22/38 同主体、15/38 跨主体同模式，
  所以同一个主体的第二条必须跨任务家族、同一个需求模式最多两条（2026-09-16 方案 B）。
  多建出来的目录注定互相判雷同（实测 48/49 条的批次废弃 44%/35%）。
- 建仓前先跑：`python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --repo "<源目录>" --capacity --capacity-file`
  会把容量、主体清单与**建议类型配比**写进父目录的 `repo-capacity.json`。
- 类型配比也跟着容量走：新增能力类（`代码生成` + `功能迭代`）不超过一半，其余给
  `缺陷修复`、`代码重构`、`代码理解`、`工程化`（实测废弃率分别是 43%–57% 与 0%–29%）。
- 容量不足时不要硬凑：容量 < 8 说明这个仓库太小，直接告诉用户换主体更多的仓库；
  10 个左右主体的仓库按 2 条/主体的口径是 16–22 条，这个区间就是它的正常产能；
  不要为了凑条数往同一主体塞第三条、或往同一模式塞第三条；
  用户明确要求多于容量时，建仓脚本会照建但把 `capacity_warning` 打出来，
  并要求走 `--allow-gate-failure` 才写得进工作簿。

这条 route 的细则不再内联在本文件中，必须调用平级 skill：

- `$solo-batch-repo-seed`
- `/Users/fengyin/.codex/skills/solo-batch-repo-seed/SKILL.md`

本 skill 在这条 route 里只负责：

- 确认已经唯一命中 `批量建仓路`。
- 把父目录、源目录、source number、命名规则、容量结果等上下文收束后交给 `$solo-batch-repo-seed`。
- 批量建仓时要求 `$solo-batch-repo-seed` 先算容量，再按容量的类型配比建目录；`代码生成` 与 `功能迭代` 仍按每批最多 5 个分块交替规划序号；两类主任务之后按 `缺陷修复`、`代码重构`、`代码理解`、`工程化` 的顺序排列。
- 默认数量不再固定：总条数 = 容量（≤35），各类型条数 = `repo-capacity.json` 里的 `type_mix`；只有用户显式给出某类数量时才覆盖该类的默认值。
- 默认命名沿用 `<源项目编号><序号>-<任务类型标识>-<序号>`；只有用户明确要求短编号时才切到 `--name-style dash`。
- 保留本文件 `2.4` 的 Hard Stop 优先级；如果源项目编号无法判定且用户没给 `--source-number`，先停，不要下发给子 skill。
- 接收子 skill 返回的建仓结果摘要。
- 建仓成功后继续进入 `批量首轮路`；默认先运行：
  - `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py scan --parent "<父目录>"`

除了路由判定和结果衔接，本 skill 不再重复维护批量建仓的具体步骤、命名规则、dry-run、目录结构校验和失败修复细则；这些全部以下沉后的 `$solo-batch-repo-seed` 为准。

### 6.2 批量首轮路

当用户要求对一个父目录下的多个子项目批量生成提示词时，走这条路。

这条 route 的细则不再内联在本文件中，必须调用平级 skill：

- `$solo-batch-first-prompts`
- `/Users/fengyin/.codex/skills/solo-batch-first-prompts/SKILL.md`

本 skill 在这条 route 里只负责：

- 确认已经唯一命中 `批量首轮路`。
- 如果父目录下还只有一个源代码子目录，且还没有建出任务目录和工作簿，强制回退到 `批量建仓路`，不要误进本路。
- 把父目录、Excel 路径和难度规则等上下文收束后交给 `$solo-batch-first-prompts`。
- 工作簿里有多少行就生成多少条：行数由建仓阶段的容量决定，**不要预设 46 条**，也不要在容量之外补行。
- 批量生成时要求 `$solo-batch-first-prompts` 按 `代码生成` 5 个、`功能迭代` 5 个分块交替处理；如果某一类 pending 不足 5 个，就处理该类剩余数量后继续切换；两类主任务之后按 `缺陷修复`、`代码重构`、`代码理解`、`工程化` 的顺序处理。
- 接收子 skill 返回的批量生成摘要。
- 每个任务的代码来源是它自己目录下的 `origin`；`origin` 不含 git 信息，批量生成阶段不要在这些目录里跑 `git status` / `git diff`。
- 批量生成时难度只允许 `地狱` 和 `困难` 两档，不允许出现 `简单` 或更低档位；默认整批按 `地狱` 出题，不再按编号奇偶对半分配，只有确实达不到地狱档硬要求时才降为 `困难` 并在备注写明原因。
- 难度不是标签：`地狱` 必须压住 2 到 3 个咬合能力点、至少 2 类非开心路径和 1 项跨状态一致性要求；`困难` 必须包含 2 个能力点、至少 1 类非开心路径和 1 项与既有功能的联动约束；具体口径以下沉后的 `$solo-batch-first-prompts` 为准。
- `缺陷修复` 批量题必须先过平台「过于简单」拒收口径，按 `references/01_Bug修复任务执行手册.md` 的「难度下限」一节出题：至少两个咬合缺陷点，或一个共享根因在两个以上入口都能看到；不允许出「加一条校验再给一句提示」这类单点题。
- 批量生成必须跑到难度下限检查 `ok: true` 才算成功：`python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_difficulty.py --parent "<父目录>"`（默认全部任务类型）。`缺陷修复` 命中两项及以上「过于简单」特征时必须换角度重出；`代码生成` / `功能迭代` 的命中项进 `needs_review`，要逐条复核并把依据写进工作簿 `备注`（以 `难度复核: ` 开头），不接受只加限定词，也不接受不看这份清单。
- 批量生成必须跑到同仓库主题去重 `ok: true` 才算成功：`python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --parent "<父目录>" --repo "<父目录>/<任意任务目录>/origin" --require-ledger --write-ledger`；**台账缺失、主体/需求模式识别为空、同主体超过 2 条、同模式超过 2 条、同主体同家族、同主体同对象、单仓库超过 35 条、句式指纹或新增能力类占比超线都是硬拦**（判据对 38 对真实判废对召回 97.4%），`review_required_labels` 要逐条写出与最近邻的差异。台账写在父目录的 `repo-theme-ledger.json`（含每条的主体、需求模式与句式指纹），后续批次继续在它上面比，做到同仓库全局去重。
- 同一批里**主体与需求模式各自只能出现一次**：这是硬约束，不是建议。撞了就换主体、换模式或换仓库，不要靠改措辞、换同义词、把模式标签改个说法来绕过。
- 工作簿里的 `任务类型` 与验收侧口径要对上：出题侧沿用 `缺陷修复` 这个内部标签（目录名 `-bug-`、难度过滤都用它），验收侧 `TASK_TYPE_MAP` 负责把它映射成平台规范值 `Bug修复`。任何一侧新增或改名，必须同步另一侧的映射表与回归用例，否则整批会卡在提交组装阶段。
- 批量生成必须跑到提示词重复检查 `ok: true` 才算成功：文字重复率、语义近似度都要 `<= 20%`，复合需求交叉重叠为 0 对；命中时必须换角度重出，不接受只改同义词。
- 批量生成成功后，不自动投递任何提示词，只提示用户按需手动运行批量投递。
- 给用户的最终回复仍只简要说明：
  - Excel 位置。
  - 生成数量。
  - 跳过数量。
  - 因找不到真实缺陷或真实重构目标而改判任务类型的条数。
  - 因命中「过于简单」难度下限而换角度重出的条数。
  - 建议下一步。
  - 未处理原因。

除了路由判定、前置分流和结果衔接，本 skill 不再重复维护子文件夹识别、Excel 断点续跑、批量去重、防撞规则和多文件联动要求；这些全部以下沉后的 `$solo-batch-first-prompts` 为准。

### 6.3 批量投递路

当父目录下已经存在 `solo-create-prompts.xlsx`，且用户发送编号范围、要求打开对应 Trae 窗口、粘贴提示词并自动发送时，走这条路。

执行规则：

- 先确认当前目录或用户提供路径是父目录。
- 从 Excel 的 `编号` 列匹配用户给出的范围，不按聊天里的序号猜。
- 只处理 `提示词` 列非空的记录。
- 每个记录都先打开对应子文件夹的 Trae 窗口，再把该行提示词粘贴到 Trae 聊天窗口并发送。
- 每次发送前都必须确认 Trae 前台窗口标题包含目标子文件夹名称；否则按 `2.3` 跳过。
- 每次按回车发送后都要等待 Trae 接收完成，再切到下一个项目。
- 成功发送后把该行 `状态` 更新为 `已发送`。
- 发送脚本完成后必须复查 Excel 对应记录的 `状态`。
- 如果某条已经发送但状态没有变成 `已发送`，立刻补写状态和备注。
- 如果用户只说“前几个”，按 Excel 中编号升序选取。
- 用户没有指定数量时，默认投递前 6 个。

按范围投递命令：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/send_batch_prompts_to_trae.py --parent "<父目录>" --range "22-27"`

按数量投递命令：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/send_batch_prompts_to_trae.py --parent "<父目录>" --limit 6`

投递后核对命令：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py pick --parent "<父目录>" --range "<已投递编号范围>"`

必要时补写状态：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py update --parent "<父目录>" --folder "<子文件夹名称>" --status "已发送" --note "已自动发送到 Trae"`

如果 Trae 没有聚焦聊天输入框，先停下并告诉用户需要把光标放进 Trae 聊天输入框后再重试；不要把提示词发送到不确定输入区域。

### 6.4 已生成批次超容量的整改（撞格批次）

一批提示词已经写完、但按当前口径撞格（同主体第二条、同模式第二条、超容量）时，按下面顺序整改。
2026-09-16 用这套流程整过 cc-9900003 那批（20 条 → 12 条），每一步都踩过坑，别跳。

1. **先算容量与剩余题位**，不要凭感觉删：
   `check_repo_theme.py --repo "<父目录>/<任意任务目录>/origin" --capacity` 拿容量；
   再跑一次主题去重，看 `module_counts`（哪些主体用了）与 `mode_counts`（哪些模式用了），
   两边一减就是剩余题位：**主体还能放几条 = 2 − 该主体已有条数（第 2 条必须跨家族）；模式同理**。
   剩余题位 = 这两者取小后汇总，不是「容量减行数」——豁免题型（工程化/代码理解/代码重构）不占主体位。
2. **备份，再动手**：工作簿、`repo-theme-ledger.json`、`repo-feature-points.json`、
   `prompt-generation-manifest.json` 各复制一份带时间戳的副本；同仓库台账
   （`~/.codex/repo-theme-ledgers/<owner-repo>.json`）也备份。
3. **多出来的题位移到归档目录，不要删**：在父目录建 `.dropped-over-capacity/`（点开头，
   建仓脚本会当隐藏目录跳过），把要撤掉的整目录搬进去，工作簿里对应行同时去掉。
4. **清空并重建两份台账**（最容易漏的一步）：被撤掉的题如果还留在台账里，
   它们的 label 不等于当前批次，会被当成「别的批次的题」继续占主体位与模式位，
   于是你刚删完就报一堆同主体超额/同模式重复。做法是先删父目录台账与同仓库台账，
   再跑 `check_repo_theme.py --parent "<父目录>" --repo "<origin>" --write-ledger --write-feature-points` 重建。
5. **重排编号与子目录名**：沿用 `<源项目编号><序号>-<任务类型标识>-<序号>`，序号从 1 连续排。
   改名用**两阶段**（先全部改成临时名，再改成目标名），否则新名与旧名互撞会失败。
6. **收尾核对**：四道闸全跑到 `ok`（主题去重、难度、查重、提交侧 `prompt_risks`），
   再跑 `batch_prompt_workbook.py scan --parent "<父目录>"`，要求「发现目录数 = 工作簿行数、待生成 0」——
   待生成不为 0 说明还有目录没配上题位。
7. **补题**：如果还有剩余题位要补新题，按题位表写；写之前确认这一条的主体与模式都没被占，
   写完照常过四道闸。补题时优先用没占用的主体（例如全部题位都用满时，豁免题型也算占一个模式位）。

## 7. 验收分级

验收是 route 内阶段，不是单独 route。进入验收前，先按 `2.1` 判断本次走 `带 Session 提交态` 还是 `无 Session 只验收态`。

如果本次验收对应的提示词记录了隐藏约束，必须先取回同一份清单再验收：

- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/query_prompt_history.py --project-path "<项目路径>" --task-type "<任务类型>"`
- 取回后按清单逐条核对，隐藏约束不满足时，和题面明示要求不满足同等处理，直接计入 `未完成` 证据。
- 隐藏约束只用于验收核对，不得在最终输出里把它改写成新的需求，也不要把它写进 `【提示词】`。

最高优先级判断：

- 如果通过代码变更已经能明确判断结果没做对，直接判 `未完成`，不要启动浏览器确认。
- 如果通过代码变更已经足以支持 `未完成` 结论，即使任务本身是前端页面或强交互任务，也不要启动服务，不要进入浏览器，不要为了补截图、补复现或补正证据再做页面验证。
- 如果通过代码变更已经初步判断结果做对了，不能直接判 `已完成`；必须启动服务并使用 `@浏览器`、`@chrome` 或 Safari 严格实操后，才能给 `已完成`。
- 如果是纯后端项目，不启动浏览器；优先通过代码、接口逻辑、数据流、测试或命令行结果判断。
- 如果是前端页面或交互任务，只要代码侧初判为完成，就默认进入浏览器验证。
- 只有代码证据不够、且任务确实依赖前端交互或页面表现时，才允许在“未完成”和“暂时无法判定完成”之间继续补浏览器证据。
- 只要浏览器实操里有一步无法稳定复现预期结果，或需要主观替实现解释，默认直接判 `未完成`。

### 7.1 轻验收

命中任一情况即可：

- 代码改动已经足够明确说明结果没做对。
- 代码变更已经覆盖任务关键路径，继续启动浏览器不会增加判断依据。
- 项目是纯后端、脚本、CLI、服务端接口或数据处理任务。
- 当前环境起不来，但代码侧证据已经足够强。

必须做：

- 看相对基线改动。
- 读真正新增或继续修改的文件。
- 按需要运行后端测试、类型检查、接口相关命令或最小命令行验证。
- 只要本次轻验收已经能判 `未完成`，后续就不要再启动服务，不要再进入 `标准验收` 或 `重验收` 补浏览器证据。
- 直接给完成判断。

### 7.2 标准验收

标准验收只允许用于下面两类场景：

- 代码侧已经初步判断前端结果做对，但还缺少页面实操正证据，无法直接输出 `已完成`。
- 代码侧证据还不够，当前既不能直接判 `未完成`，也不能直接判 `已完成`，而本地可运行、确实需要结果确认。

只要不满足这两个前提之一，就不要进入标准验收。

进入这一路之前必须先排除一种情况：

- 如果代码侧已经足以判 `未完成`，即使用户提了页面、交互、浏览器或前端，也不能进入标准验收；必须留在轻验收直接给 `未完成`。
- 如果还试图进入标准验收，直接视为命中 `2.10`，立即停止后续服务启动和浏览器动作。

必须做：

- 看相对基线改动。
- 判断项目类型。
- 前端任务启动服务并打开页面；纯后端不走浏览器。
- 如果默认端口已被占用，必须继续往后顺延端口直到找到可用端口，再用该端口启动服务。
- 给出最终结论后，必须停掉这次验收阶段新启动的服务进程。
- 用 `@浏览器`、`@chrome` 或 Safari 至少走一遍核心路径，并补做最容易漏掉的后续动作验证。
- 给固定格式结论。

### 7.3 重验收

重验收只允许建立在一个前提上：

- 代码侧还不能直接判 `未完成`，并且任务价值主要落在真实页面交互结果，必须靠更重的实操覆盖来确认是否真的 `已完成`。

只有先满足这个前提，才允许再看下面的升级条件。

命中任一情况时升级到重验收：

- 代码侧初判为完成，但任务价值主要落在页面交互结果。
- 前端强交互任务。
- 路由、滚动、回跳、多步骤表单。
- 列表详情联动、筛选分页联动、跨区域联动。
- 用户明确要求用 Codex Browser 实操。
- 用户贴了 Trae 过程信息。
- 上一次已经证明“只看代码不够，必须真点一遍”。
- 用户明确要求“验收一定要特别严格”。

进入这一路之前也必须先排除一种情况：

- 如果代码侧已经足以判 `未完成`，不要因为“任务价值主要落在页面交互结果”或“用户要求特别严格”就升级到重验收；必须直接停在代码验收结论，不启动服务，不进入浏览器。
- 如果还试图升级到重验收，直接视为命中 `2.10`，立即停止后续服务启动和浏览器动作。

重验收必须额外执行：

- 默认先使用 `@浏览器`，并按 `browser:control-in-app-browser` 真实连接 `iab` 后端；如果 `iab` 当前不可用，则按 `chrome:control-chrome` 改用 Chrome extension 后端；如果 Chrome extension 也不可用，再用 Computer Use 操作 Safari；不要改用别的浏览器代替默认路径。
- 如果 `browser_policy=browser_only`，则必须只使用 `@浏览器`；当前线程没有 Browser 能力或 Browser 不可用时，按 `2.9` 停止。
- 如果默认端口已被占用，必须继续往后顺延端口直到找到可用端口，再用该端口启动服务。
- 先按主路径完整走一遍，再做至少 3 类补充检查：刷新后重试、切换数据或路由后重试、返回上一步再进入重试。
- 对表单、筛选、分页、弹窗、详情抽屉、标签切换、滚动加载、登录态依赖、草稿态依赖，命中哪个测哪个，不要因为首个路径成功就停止。
- 必须主动寻找反例，不要只记录成功路径。
- 只要有一个关键反例成立，就直接判 `未完成`。
- 给出最终结论后，必须停掉这次验收阶段新启动的服务进程。

### 7.4 浏览器规则

- 浏览器不是“代码初判未完成”时的默认动作；代码侧已经能判未完成，就直接给结论。
- 只要代码侧已经能判 `未完成`，不仅不要启动浏览器，也不要启动本地服务；浏览器规则只服务于“验证是否已完成”或“代码证据不足时补结果证据”，不能用来反向确认已经成立的 `未完成`。
- 纯后端项目、CLI 项目、数据处理项目、脚本项目不启动浏览器。
- 如果通过代码变更已经初步判断前端任务完成，必须启动服务并使用 `@浏览器`、`@chrome` 或 Safari 实操验证。
- 如果 `browser_policy=browser_only`，必须启动服务并使用 `@浏览器` 实操验证；不允许 `@chrome` 或 Safari 代替。
- 启动服务时如果预期端口已被占用，必须继续往后尝试新端口，直到找到可用端口；不要因为端口冲突直接放弃验收，也不要停掉别的项目正在使用的端口。
- 服务启动失败、页面起不来、关键链路无法进入时，前端任务不能判 `已完成`。
- 进入浏览器验收前，先按 `browser:control-in-app-browser` 真实尝试连接 `iab` 后端来确认当前线程是否有 `@浏览器` 能力；如果没有，默认策略按 `2.8` 先尝试 Chrome extension 后端，再按需要用 Computer Use 操作 Safari；强制策略按 `2.9` 停止。
- 需要浏览器时，默认优先使用 `@浏览器`。
- 需要浏览器时，不要自行改用 Google Chrome、Chrome 或其他系统浏览器。
- 如果 `@浏览器` 不可用、连接失败，或被能力边界客观卡住，默认策略先按 `chrome:control-chrome` 降级到 Chrome extension；如果 Chrome extension 也不可用或被能力边界客观卡住，再用 Computer Use 操作 Safari；强制策略按 `2.9` 停止。
- 如果 `@chrome` 和 Safari 都不可用或被客观卡住，再停止并说明阻塞原因。
- 当前这次验收只要新启动过本地服务，就必须在输出最终结论之后立即停掉该服务；这条是强约束，不要遗漏，也不要把服务留在后台。
- 浏览器验收时至少覆盖这 6 个检查点里与任务相关的部分：页面进入、关键操作、结果落点、刷新后保持、返回或切换后的再次进入、异常输入或空数据下的表现。
- 不能只打开页面看静态结果；只要任务涉及点击、输入、切换、弹层、滚动、异步加载、路由跳转、列表详情联动，就必须实操。
- 不能因为“看起来差不多”给 `已完成`；必须拿到明确正证据。
- 找不到明确正证据时，默认不输出 `已完成`；能确认缺陷就判 `未完成`，证据仍不足才判 `暂时无法判定完成`。

### 7.5 验收闭环提交规则

验收闭环分两种模式执行，不要混用：

- `带 Session 提交态`：用户已提供格式合法的 `Trae Session ID`，允许执行提交、push、带 `Commit ID` / `Trae Session ID` 的输出。
- `无 Session 只验收态`：用户没有提供合法 `Trae Session ID`，或明确表示这次不提供；只做验收，不做任何 git 提交和 push。

必须满足：

- 验收结论里的 `【提示词】` 必须严格对应“当前这次实际被验收的提示词”，不能回填别的轮次内容。
- 如果当前验收的是修复轮次结果，`【提示词】` 必须填写该次修复提示词，不能填写首轮提示词，也不能把首轮提示词和修复提示词拼在一起。
- 如果一次会话里先有首轮提示词，后又生成过修复提示词，后续所有围绕修复结果的验收、提交核对和最终输出，都以最新那条有效修复提示词作为当前验收依据。
- 如果当前进入修复轮次，必须先把该次修复提示词作为独立记录追加到提示词 Excel；禁止把修复提示词写回或覆盖原有主提示词记录。
- 如果当前是 `带 Session 提交态`，且工作区里存在围绕当前提示词的改动，不管最终结论是 `已完成`、`未完成` 还是 `暂时无法判定完成`，都必须先用合法 `Trae Session ID` 提交一次，再尝试 push 到远程，然后再输出验收结论。
- `带 Session 提交态` 下，用于 push 的提交 message 必须严格等于这个合法 `Trae Session ID`。
- `带 Session 提交态` 下，如果当前分支上还没有以该合法 `Trae Session ID` 为 message 的提交，先补提交再 push。
- `带 Session 提交态` 下，必须核对“当前有效 `Trae Session ID` = `git commit` message = 最终质检结论里的 `【Trae Session ID】`”；三者任一不一致，都不能继续给最终验收结论。
- `带 Session 提交态` 下，如果已经有同 message 的提交，但工作区里还有围绕当前提示词的新改动，默认先确认是否允许 amend；用户没有明确允许 amend 时，不要跳过“提交并 push”动作。
- 如果当前是 `无 Session 只验收态`，一定不能执行 `git commit`、`git push`、`git amend`，也不要因为工作区有改动就强行补提交流程。
- `Repo URL` 必须取当前仓库远端 `origin` 对应的 GitHub 仓库地址，统一规范化为 `https://github.com/owner/repo`，只输出最终 URL 本体，禁止带 `.git` 后缀。
- `带 Session 提交态` 下，`Commit ID` 必须填这个已 push 到远端的完整 40 位提交哈希。
- `带 Session 提交态` 下，`Trae Session ID` 必须原样回填当前上下文里可用、且通过格式校验的原始值。
- `无 Session 只验收态` 下，最终输出里不要展示 `Commit ID` 和 `Trae Session ID`。
- 不管当前是哪种验收模式，最终输出前都必须统计“这次修改的文件个数”，并把它作为单独字段输出。
- `修改文件个数` 必须按当前这次验收实际涉及的修改文件集合统计，输出整数值。
- 统计口径要前后一致：优先按当前工作区里围绕本次提示词的已修改文件集合统计；如果当前是 `带 Session 提交态` 且改动已经提交，也可以按这次被验收提交对应的文件集合统计，但最终只允许输出一个整数。
- 统计时不要把 `node_modules`、构建产物、日志、截图、临时文件或与本次提示词无关的环境文件混进来；只统计围绕这次提示词实际改动的源码、配置、样式、测试、脚本和文档文件。
- 如果当前是 `带 Session 提交态`，且因为远端不可达、权限不足、仓库未配置 upstream 或其他 push / remote 问题导致没法正常 push，立即命中 `2.6`；先告知用户问题并恢复正常推送，恢复前不要输出验收结果。
- 只要没有命中 `2.6`，在最终验收模板发给用户前必须调用 `$solo-acceptance-results` 回填结果 Excel；回填不改变原有聊天输出字段，也不要在最终模板里新增 Excel 路径或回填状态。
- `$solo-acceptance-results` 必须写当前子项目目录的直接父目录里的独立结果 Excel，默认文件名 `solo-create-acceptance-results2.xlsx`；例如 `.../solo-6600021/66000212-codegen-2` 必须写到 `.../solo-6600021/solo-create-acceptance-results2.xlsx`；如果第一次不存在就新建，表头必须和 `/Users/fengyin/项目/字节/solo/数据中转/solo-create-acceptance-results2.xlsx` 一致。
- 回填给 `$solo-acceptance-results` 的 `Repo ID` 必须是 `ybl-<数字编号>-<序号>` 标准格式；不能从项目目录名解析出标准值时，必须显式传入标准 `repo_id`，否则回填脚本会失败，禁止原样写入目录名。
- 回填输入必须来自当前 `acceptance_context`、`final_output` 和 `$solo-dissatisfaction` 结果，不要重新改写验收结论。
- 如果当前是修复轮次验收，回填输入里的 `prompt_text` 必须是当前有效修复提示词，`task_type` 必须按修复轮次口径写入；结果 Excel 必须追加新的修复轮次记录，不能覆盖主轮次验收记录。
- 如果 `completion != 已完成`，回填输入还必须携带 `$solo-dissatisfaction` 校验所需的 `next_prompt`、过程证据、产物证据、模型自身责任依据和 `environment_issue_excluded=true`；回填脚本会再次运行校验，校验失败时禁止写入 Excel。
- 如果 `$solo-acceptance-results` 回填失败，先修复回填问题并重试；禁止改写到 `solo-create-prompts.xlsx`，禁止静默跳过回填后结束验收流程。

### 7.6 过程验收触发条件

只有当用户这次消息里真的贴了 Trae 过程信息，才做过程验收。

过程验收重点方向：

- 理解 prompt 是否偏了。
- 目标是否明确。
- 推理过程是否精简。
- 输出是否正常。
- 任务规划是否合理。
- 工具使用是否合理高效。
- 操作过程是否合理。
- 有没有高危操作或越界。
- 总结是否正确。
- 有没有虚假完成。

如果用户没贴过程内容，就不要凭空补过程记录。但只要结果不满意，`过程不满意` 仍然必须写成合规结论。写法只能二选一：

- 模型行为评价。
- 更深一层的代码级 / 业务逻辑级根因。

不要把业务 Bug 直接换壳写成“实现上少了校验”“没有核对结果”“没有处理好”“没有保住状态”这类空话。

## 8. 完成判断

完成状态只允许这 3 种：

- `已完成`
- `未完成`
- `暂时无法判定完成`

判断标准优先看：

- 核心行为是不是真的存在。
- 有没有明显交互问题。
- 有没有破坏原来的阅读流或使用流。
- 如果任务和缩放、筛选、切换条目有关，顺手验证这些后续动作。

两条硬规则：

- 如果当前是 `带 Session 提交态`，只有结果满足验收要求，且对应代码已经以合法 `Trae Session ID` 为 message 提交并 push 到远程，才允许输出 `已完成`。
- 如果当前是 `无 Session 只验收态`，`已完成` 只取决于验收证据本身；不要因为没有 `Trae Session ID`、没有提交、没有 push 就阻塞完成结论。
- 只要前端任务没有经过 `@浏览器`、`@chrome` 或 Safari 严格实操，或实操覆盖不到关键链路，就不能输出 `已完成`。
- 只要验收证据同时存在“像是做了”和“仍可能没做完”的冲突，默认按 `未完成` 处理，不要替实现补乐观解释。

## 9. 不满意原因与下一轮提示词

如果 `completion != 已完成`，或用户明确表示不满意，必须生成：

- `过程不满意：...`
- `产物不满意：...`
- `下一轮提示词：修复...`

这三项不再由本 skill 直接生成。必须使用平级 skill：

- `$solo-dissatisfaction`
- `/Users/fengyin/.codex/skills/solo-dissatisfaction/SKILL.md`

强制契约：

- `solo-create` 只负责验收证据、完成状态、最终模板和字段回填。
- `solo-dissatisfaction` 负责生成并校验 `过程不满意`、`产物不满意`、`下一轮提示词`。
- `solo-create` 不允许绕过 `solo-dissatisfaction` 自行编写、补写或改写这三项。
- 如果 `solo-dissatisfaction` 判定当前问题不能靠重写不满意原因修复，必须按它的 Hard Stop 结果处理，不要硬写原因。
- 如果证据不足以生成原因，先回到验收阶段补足证据；不要凭空编造过程或产物现象。

调用 `solo-dissatisfaction` 前，必须收束下面的输入包：

```text
mode: single_acceptance | batch_reason_rewrite
completion: 未完成 | 暂时无法判定完成
prompt_text: 当前这次验收对应的提示词
acceptance_evidence: 验收里看到的直接证据
failed_requirement: 没满足的需求点
user_visible_issue: 用户能看到、点到或感受到的问题；无法直接判断时写空
code_or_logic_root_cause: 代码级或业务逻辑级根因；没有把握时写空
process_trace_evidence: 过程证据，必须包含具体步骤、工具调用、命令、文件改动或验证动作
artifact_evidence: 产物证据，必须包含文件名、函数名、功能点、报错信息、漏掉的具体需求或可复现现象
model_fault_basis: 为什么这是模型自身规划、工具使用、指令遵循、幻觉、验证或纠错能力造成的问题
environment_issue_excluded: true | false
has_trae_process: true | false
trae_process_issue: 只有用户本次贴了 Trae 过程时才填写
history_similar_reasons: 最近 0 到 5 条同类原因
allowed_target_column: 批量改表时本次允许修改的列；单项目为空
```

接收 `solo-dissatisfaction` 输出后，必须立即运行校验脚本：

```bash
python3 /Users/fengyin/.codex/skills/solo-dissatisfaction/scripts/validate_dissatisfaction.py --stdin
```

校验输入必须是：

```json
{
  "process": "过程不满意正文，不含标签",
  "product": "产物不满意正文，不含标签",
  "next_prompt": "修复...",
  "mode": "single_acceptance",
  "allowed_target_column": "",
  "process_evidence": "第几步/工具调用/命令/文件改动/验证动作",
  "product_evidence": "文件/函数/功能/报错/漏掉的需求",
  "model_fault_basis": "模型自身错误的依据",
  "environment_issue_excluded": true,
  "history_similar_reasons": []
}
```

校验失败时：

- 不允许输出当前版本。
- 不允许只删几个词后沿用原句骨架。
- 必须带着失败项重新使用 `solo-dissatisfaction` 整段重写。
- 只有校验通过后，才允许进入 `11.3 输出前自检` 和最终模板输出。

最终模板里仍按 `11.2` 输出：

```text
过程不满意：...
产物不满意：...
```

以及：

```text
修复...
```

## 11. 输出契约

### 11.1 首轮提示词场景

如果当前是在生成首轮提示词，最终只输出提示词正文，不要夹带：

- 任务类型。
- 基线状态。
- 去重说明。
- 过程说明。
- 额外总结。

### 11.2 验收或修复场景

如果当前是在后续修复提示词或验收场景，必须使用唯一固定模板。所有 key 都必须用 `【】` 包裹，每个 value 都必须单独放进一个 fenced code block。

如果当前是 `带 Session 提交态` 且命中 `2.6`，不要输出下面模板；先告知 push / remote 问题，等恢复正常推送后再继续验收。

这里的 `【提示词】` 还有一条强规则：

- 首轮结果验收时，填写首轮提示词。
- 修复结果验收时，填写当前这次修复提示词。
- 不允许在修复轮次验收里继续回填首轮提示词。
- 不允许把首轮提示词当成“背景提示词”混写进 `【提示词】`。

#### 完成版（带 Session 提交态）

【Repo URL】
```text
https://github.com/owner/repo
```
【Commit ID】
```text
已 push 到远程、且 message 等于 Trae Session ID 的完整 40 位提交哈希
```
【Trae Session ID】
```text
当前上下文中的有效原始值
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
已完成。
```

#### 未完成版

【Repo URL】
```text
https://github.com/owner/repo
```
【Commit ID】
```text
已 push 的完整 40 位提交哈希；如果没有可填，写 无。
```
【Trae Session ID】
```text
当前上下文中的有效原始值
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
未完成。
```
【不满意原因】
```text
过程不满意：...
产物不满意：...
```
【下一轮提示词】
```text
修复...
```

#### 暂时无法判定版

【Repo URL】
```text
https://github.com/owner/repo
```
【Commit ID】
```text
已 push 的完整 40 位提交哈希；如果没有可填，写 无。
```
【Trae Session ID】
```text
当前上下文中的有效原始值
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
暂时无法判定完成。
```
【不满意原因】
```text
过程不满意：...
产物不满意：...
```
【下一轮提示词】
```text
修复...
```

#### 完成版（无 Session 只验收态）

【Repo URL】
```text
https://github.com/owner/repo
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
已完成。
```

#### 未完成版（无 Session 只验收态）

【Repo URL】
```text
https://github.com/owner/repo
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
未完成。
```
【不满意原因】
```text
过程不满意：...
产物不满意：...
```
【下一轮提示词】
```text
修复...
```

#### 暂时无法判定版（无 Session 只验收态）

【Repo URL】
```text
https://github.com/owner/repo
```
【提示词】
```text
刚刚这次验收对应的提示词；如果当前验收的是修复轮次，这里必须是该次修复提示词。
```
【修改文件个数】
```text
这次验收实际涉及的修改文件个数，输出整数。
```
【任务完成情况】
```text
暂时无法判定完成。
```
【不满意原因】
```text
过程不满意：...
产物不满意：...
```
【下一轮提示词】
```text
修复...
```

### 11.3 输出前自检

最终输出前，必须逐项自检：

- route 是否唯一且与执行过程一致。
- 当前输出是否命中 `Hard Stops`。
- 当前是否已经正确判定本次属于 `带 Session 提交态` 还是 `无 Session 只验收态`。
- 当前 `browser_policy` 是否已经按本次用户要求正确设定。
- 当前是否已经正确识别本次是默认可见浏览器模式，还是 `acceptance_browser_mode=headless`。
- 如果本次已经通过代码侧证据判定 `未完成`，是否确实已经命中并遵守 `2.10`，没有启动本地服务，也没有进入任何浏览器验收。
- 当前这次浏览器验收是否实际使用了 `@浏览器`、`@chrome`、Safari，或用户指定的无头浏览器模式；如果是强制策略，是否已经严格只使用 `@浏览器`；如果不满足，是否已经按 `2.7`、`2.7.1`、`2.9` 或 `2.9.1` 停止。
- 如果当前验收阶段新启动了本地服务，是否已经在端口被占用时自动顺延到可用端口，而不是卡在冲突端口。
- 如果当前验收阶段新启动了本地服务，是否已经在准备输出最终结论前安排并执行停服，而不是把服务遗留在后台。
- 当前这次验收是否已经通过 `$solo-acceptance-results` 回填独立结果 Excel，且没有把验收结果写入 `solo-create-prompts.xlsx`。
- `$solo-acceptance-results` 是否返回 `ok: true`，且写入文件不是生成提示词 Excel。
- 输出级内容是否含有禁词。
- 如果本次是在生成首轮或修复提示词，是否已经运行 `check_prompt_dedup.py` 并拿到 `ok: true`，且文字重复率、语义近似度都 `<= 20%`、复合需求交叉重叠为 0 对。
- 如果本次是在生成首轮或修复提示词，是否已经运行 `check_prompt_difficulty.py` 并拿到 `ok: true`，没有命中 2 项及以上「过于简单」特征。
- 如果本次是在生成首轮或修复提示词，是否已经运行 `check_repo_theme.py` 并拿到 `ok: true`（主体、需求模式、配比、句式指纹都在线内），`review_required_labels` 是否逐条写出了与最近邻的差异。
- 如果本次生成的是缺陷修复提示词，是否满足结构下限：至少两个咬合缺陷点或一个共享根因跨两个以上入口，且题面落到两个以上入口。
- 如果本次生成的是 `缺陷修复` 提示词，是否已经按手册难度下限核对：至少两个咬合缺陷点或一个共享根因跨两个以上入口、至少两个相关文件层级、至少 2 类非开心路径、至少 1 项跨状态一致性，且没有写成「加一条校验再给一句提示」。
- 如果当前输出包含 `过程不满意`、`产物不满意` 或 `下一轮提示词`，三者是否确实由 `$solo-dissatisfaction` 生成，而不是在本 skill 内自行编写。
- 如果当前输出包含 `过程不满意`、`产物不满意` 或 `下一轮提示词`，是否已经运行 `/Users/fengyin/.codex/skills/solo-dissatisfaction/scripts/validate_dissatisfaction.py` 且返回 `ok: true`。
- 如果 `solo-dissatisfaction` 或校验脚本判定当前版本无效，是否已经整段废弃并重写，而不是只替换几个词继续沿用原句结构。
- `过程不满意`、`产物不满意`、`提示词` 每行是否以标点结尾。
- `修改文件个数` 是否已经统计完成，且是否紧跟在 `【提示词】` 后面输出。
- 如果输出了 `下一轮提示词`，它是否以 `修复` 开头。
- 如果当前是 `带 Session 提交态`，验收输出里的 `Repo URL`、`Commit ID`、`Trae Session ID`、`提示词`、`修改文件个数` 是否都对应当前这次验收。
- 验收输出和 `$solo-acceptance-results` 回填 Excel 的 `Repo URL` 是否都已经规范化为不带 `.git` 后缀的 GitHub HTTPS 页面 URL。
- 如果当前是 `无 Session 只验收态`，最终输出是否已经去掉 `Commit ID` 和 `Trae Session ID`。
- 如果当前是修复轮次验收，`【提示词】` 是否已经切换为当前修复提示词，而不是首轮提示词。
- 如果当前已经有多条修复提示词，`【提示词】` 是否对应最新且当前有效的那一条。
- 如果当前是修复轮次验收，是否已经确认提示词 Excel 追加了新的修复提示词记录，而不是复用主提示词记录。
- 如果当前是修复轮次验收，结果 Excel 是否已经新增修复轮次验收记录，而不是覆盖主轮次验收记录。
- 如果当前是 `带 Session 提交态`，用户输入的 `Trae Session ID`、`git commit` message、最终输出里的 `【Trae Session ID】` 是否完全一致。
- 如果当前是 `无 Session 只验收态`，是否确实没有执行 `git commit`、`git push`、`git amend`。
- `solo-dissatisfaction` 输入包是否已经包含过程轨迹证据、产物证据、未满足需求和模型自身责任依据，且没有凭空编造 Trae 过程或把环境 / 网络波动写成模型问题。
- 是否粘贴了异常类名、堆栈原文、控制台原文、日志转储或可识别的报错文本骨架到输出级内容。
- `过程不满意：`、`产物不满意：` 分段标签是否都只出现 1 次。
- 如果当前是在批量改表，是否已经确认本次质检问题真的属于“只改不满意原因即可修复”。
- 如果当前是在批量改表，是否已经校验除目标列外的所有单元格保持不变。

## 12. 附录

### 12.1 手册对应关系

- `缺陷修复`：`references/01_Bug修复任务执行手册.md`
- `代码生成`：`references/02_代码生成任务执行手册.md`
- `功能迭代`：`references/03_Feature迭代任务执行手册.md`
- `代码理解`：`references/04_代码理解任务执行手册.md`
- `代码重构`：`references/05_代码重构任务执行手册.md`
- `工程化`：`references/06_工程化任务执行手册.md`

### 12.2 基线和历史脚本

优先顺序保持不变：

- `scripts/record_round_baseline.py`
- `scripts/compare_round_baseline.py`
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/query_prompt_history.py --project-path . --task-type "<任务类型>"`
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/record_prompt_history.py --project-path . --task-type "<任务类型>" --angle "<这次的切入点>" --difficulty "<地狱|困难>" --hidden-constraints "<隐藏约束中文描述；没有就传空>" --prompt "<最终提示词>"`
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_dedup.py --parent "<父目录>" --include-history`（提示词重复硬阈值检查：文字重复率、语义近似度 `<= 20%`，复合需求交叉重叠 0 对）
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_prompt_difficulty.py --prompts-file "<临时文件>"`（难度下限检查：命中 2 项及以上「过于简单」特征即拒收；批量场景用 `--parent "<父目录>"`，**默认覆盖全部任务类型**；`缺陷修复` 硬拦，`代码生成` / `功能迭代` 的命中项进 `needs_review` 并逐条写依据）
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --parent "<父目录>" --repo "<父目录>/<任意任务目录>/origin" --require-ledger --write-ledger --write-feature-points`（同仓库主题去重：台账缺失、主体/模式识别为空、同主体超过 2 条、同模式超过 2 条、同主体同家族、同主体同对象、单仓库超过 35 条、句式指纹/新增能力类占比超线都硬拦；判据对 38 对判废对的召回 97.4%；同主体或需求模式有交集的进复核清单，并带 `review_list` 最近邻。判词回归默认加载 `tests/fixtures/rule-c-corpus.json`。台账 `repo-theme-ledger.json` 与题位表 `repo-feature-points.json` 落在父目录，同仓库台账落在 `~/.codex/repo-theme-ledgers/`，跨批次、跨父目录继续累计）
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/check_repo_theme.py --repo "<源目录>" --capacity --capacity-file`（建仓前第一步：算这个仓库能出多少条题 = `min(单仓库上限 35, 主体数 × 2, 需求模式数 × 2)`，给出建议类型配比，写 `repo-capacity.json`；建目录数与 Excel 行数都按它来）
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/create_batch_local_tasks.py --parent "<父目录>" --dry-run`（批量建仓：不传数量时按容量自动分配，计划数超过容量时返回 `capacity_warning`）
- `python3 /Users/fengyin/.codex/skills/solo-creater/scripts/batch_prompt_workbook.py update --parent "<父目录>" --folder "<子文件夹名称>" --prompt "<最终提示词>"`（写提示词前自动跑上面三道闸，不通过拒写；通过后把闸门版本写进备注并落 `prompt-generation-manifest.json`）

### 12.3 多项目并行

- 一个项目只对应一个工作目录。
- 一个项目只对应一组端口。
- 一个项目只对应自己的基线记录。
- 一个项目只对应内置浏览器里的一组固定标签页。

如果两个项目端口相同，默认从原端口 `+1` 往后找可用端口，不要停已有服务。

只要本次验收为了当前项目新启动过服务，输出结论之后必须把这次新启动的服务停掉；不要因为端口是后来顺延找到的，就把该进程留在后台。

如果两个项目依赖同一个登录态、同源浏览器存储、同一套会互相污染的数据环境，或当前没法可靠区分标签页，就不要直接并行验收。
